[pytest]
pythonpath = src
addopts = --html=reports/report.html --self-contained-html --capture=tee-sys
testpaths = tests
markers =
    noreport: exclude from UI HTML report on Windows
    ui: tests that require real desktop and rcgui.exe
    e2e: end-to-end flows (UI)